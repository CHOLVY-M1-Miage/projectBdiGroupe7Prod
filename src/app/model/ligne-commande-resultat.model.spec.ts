import { LigneCommandeResultat } from './ligne-commande-resultat.model';

describe('LigneCommandeResultat', () => {
  it('should create an instance', () => {
    expect(new LigneCommandeResultat()).toBeTruthy();
  });
});
