import { LigneCommandeSearch } from './ligne-commande-search.model';

describe('LigneCommandeSearch', () => {
  it('should create an instance', () => {
    expect(new LigneCommandeSearch()).toBeTruthy();
  });
});
